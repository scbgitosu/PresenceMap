# Python-only deps for the HP field collector.
# Qt platform plugins and Wi-Fi tools are system packages — see README.md
# ("HP Ubuntu setup") for the matching `sudo apt install ...` line.
PyQt5>=5.15
pandas>=2.0
opencv-python-headless>=4.9
