"""Read-only adapters for v1 PresenceMap data.

Used by the dashboard's Legacy Viewer page to surface old ``presence_sessions/``
content. Nothing in here writes; if you need v2 artifacts, use ``shared.schemas``
and the v2 writers under ``mac_app.train.dataset``.
"""
